html
    include _head.pug
    body
        include _header.pug

        main
            div(id="formSide")
                div
                    form(action="/")
                        label(for="address")= "Address"
                        br
                        input(id="inputbox" type="text" name="address")
                        br
                        button(id="query" type="submit")= "Submit"
                div
                    ul
                        li= "The list items"
                    
            div(id="mapSide")
                div(id="map")
        
        include _footer.pug